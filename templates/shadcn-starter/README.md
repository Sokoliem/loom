# shadcn-starter

Reference starter for new loom projects. Used by `project_create({ template: "shadcn-starter" })`.

This directory is canonical reference content only — `project_create` writes the seed inline
(see `src/core/project.ts::writeShadcnStarter`) rather than copying from here. Update both if
you change the starter shape.
